{
  "name": "simple-erc20-token",
  "version": "1.0.0",
  "description": "Minimal ERC20 token dapp",
  "scripts": {
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run deploy.js --network localhost"
  },
  "dependencies": {
    "ethers": "^6.10.0",
    "hardhat": "^2.22.0"
  }
}
